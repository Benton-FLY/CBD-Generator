import {chromium,webkit} from 'playwright';
const ref=['reference/FLY RACING 27 MX JERSEY CBD 2025.10.17.xlsx','reference/FLY RACING 27 BMX JERSEY CBD 2025.10.17.xlsx'];
const cur='reference/FLY RACING 28 MX BMX JERSEY CBD SHEET (내부) 2026.09.02.xlsx';
for(const engine of [chromium,webkit]){const browser=await engine.launch({headless:true}),page=await browser.newPage({acceptDownloads:true});await page.goto('http://127.0.0.1:5175/#comparison');await page.locator('input[type=file]').nth(0).setInputFiles(ref);await page.locator('input[type=file]').nth(1).setInputFiles(cur);await page.getByText('2개 파일 · 16개 STYLE').waitFor({timeout:60000});await page.getByText('1개 파일 · 19개 STYLE').waitFor({timeout:60000});if(await page.getByText('27 RAYCE JERSEY').count()<1)throw new Error(`${engine.name()} RAYCE style missing`);await page.getByRole('button',{name:'STYLE 매칭 확정'}).click();await page.getByRole('heading',{name:'자재 매칭 검토'}).waitFor({timeout:60000});const download=page.waitForEvent('download');await page.getByRole('button',{name:'비교 Excel 추출'}).last().click();const d=await download;if(!d.suggestedFilename().startsWith('CBD_Comparison_'))throw new Error('unexpected comparison filename');await page.waitForTimeout(800);await page.reload();await page.getByRole('heading',{name:'자재 매칭 검토'}).waitFor({timeout:60000});await browser.close();console.log(`${engine.name()}: 27=16 styles, 28=19 styles, RAYCE matching, export, reload restore PASS`)}
